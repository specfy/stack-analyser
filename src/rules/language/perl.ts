import { register } from '../../register.js';

register({
  tech: 'perl',
  name: 'Perl',
  type: 'language',
  extensions: ['.pl', '.al', '.cgi', '.fcgi', '.perl', '.ph', '.plx', '.pm', '.psgi', '.t'],
});
