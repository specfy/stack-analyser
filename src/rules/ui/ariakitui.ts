import { register } from '../../register.js';

register({
  tech: 'ariakitui',
  name: 'AriaKit UI',
  type: 'ui',
  dependencies: [{ type: 'npm', name: /^@ariakit\//, example: '@ariakit/react' }],
});
