import { register } from '../../register.js';

register({
  tech: 'tanstackstart',
  name: 'Tanstack Start',
  type: 'framework',
  dependencies: [{ type: 'npm', name: '@tanstack/react-start' }],
});
